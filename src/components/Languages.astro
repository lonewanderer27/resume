---
const { languages } = Astro.props as { languages: string[] }
---

{languages && languages.length > 0 && (
  <section class="languages">
    <div class="title">Languages</div>
    {languages.map((lang) => (
      <div class="idiom">{lang}</div>
    ))}
  </section>
)}

<style lang="scss">
.languages {
  .title {
      @include header;
  }
  .idiom {
      @include body-text;
  }
}
</style>