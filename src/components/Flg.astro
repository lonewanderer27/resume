<script lang="typescript">
  function a() {
    const b = window.location.href.replace(/^https?:\/\//, "");
    const c = new Date().getTimezoneOffset();
    const d = Math.abs(c);
    const e = Math.floor(d / 60);
    const f = c > 0 ? "-" : "+";
    const g = "GMT" + f + e;

    const h = "F";
    const i = "L";
    const j = "G";
    const k = "_";
    const l = h + i + j + k;
    const m = b;
    const n = "_" + g;

    return l + m + n;
  }

  window.flg = a;
</script>
